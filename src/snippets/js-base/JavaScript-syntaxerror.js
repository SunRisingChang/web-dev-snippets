module.exports = {
  "e.toString();": {
    "prefix": "syntostring",
    "body": [
      "${1:syntaxerror}.toString()"
    ],
    "description": "toString() 方法返回一个指定的错误对象（Error object）的字符串表示。\ne.toString();"
  }
}